---
phase: 10
plan: 10-01
wave: 1
depends_on: []
autonomous: true
requirements: [TST-01, TST-02, TST-03, TST-04, TST-05]
files_modified:
  - app/scripts/phase2/rate-intent.test.ts
  - app/scripts/phase2/rate-answer.test.ts
  - app/scripts/phase2/citation.test.ts
  - app/scripts/phase2/context-builder.test.ts
  - app/scripts/phase2/query-transforms.test.ts
  - app/package.json
  - app/scripts/phase2/FINDINGS-ciclo003.md
---

<objective>
Fechar o risco "zero cobertura em lógica crítica de cotação/parsing/scoring". Testes
unitários tsx (convenção do repo: standalone, exit 0/1, padrão dos phase2/*.test.ts)
para a lógica PURA, determinística e crítica do RAG hoje sem teste. Prioridade: o motor
de cotação (detectRateIntent + formatRateAnswer), o coração do produto.
</objective>

<regra_critica>
Estes testes documentam o comportamento ESPERADO. Se um teste falhar contra o código
atual, há duas possibilidades: (a) o teste está errado → ajustar o teste; (b) o CÓDIGO
tem bug → NÃO corrigir o código silenciosamente. Registrar em
`app/scripts/phase2/FINDINGS-ciclo003.md` (id, função, input, esperado, obtido, hipótese)
e marcar o caso com `// FINDING-NN` + deixá-lo como expected-fail documentado (não quebrar
a suíte inteira por causa dele — usar um contador separado de findings). O objetivo do
ciclo é REVELAR a verdade do código, não maquiar. detectRateIntent foi marcado pelo Kimi
com 5/50 mismatches — é provável que apareçam findings reais aqui, e isso é sucesso.
</regra_critica>

<convencao_teste>
Padrão dos testes existentes (ler app/scripts/phase2/rate-unit-guard.test.ts e
domain-guard.test.ts antes de escrever):
- import do código de produção via alias `@/services/rag/...`
- contadores `passed`/`failed` (+ `findings` separado), função `check(name, cond)`
- `console.log` por caso, `process.exit(failed > 0 ? 1 : 0)` no fim (findings NÃO contam como failed)
- rodar com `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/<arquivo>.test.ts` a partir de app/
- adicionar script `"phase2:<nome>:test"` no package.json espelhando os existentes
</convencao_teste>

<tasks>

<task id="1" type="tdd">
<name>TST-01 — detectRateIntent (rate-intent.test.ts)</name>
<read_first>
- app/src/services/rag/rate-lookup.ts (detectRateIntent linhas 151-459: o que extrai — RateIntent interface linha 17; age, capital, gender, productHint, productHints, productCode, productCodes, rendaMensal, franquia, hasIntent)
- app/scripts/phase2/rate-unit-guard.test.ts (convenção de teste)
- app/eval/ragas/questions.jsonl (fraseados reais de cotação Prudential/MAG para o corpus)
</read_first>
<action>
Criar `app/scripts/phase2/rate-intent.test.ts`. Cobrir, com `detectRateIntent(question, insurer?)`:
1. POSITIVOS (hasIntent=true) com extração correta:
   - "Vida Inteira MAG homem 40 anos capital 500 mil" → age=40, gender male, capital=500000, productHint vida inteira
   - "Prudential Doencas Graves DDR5G mulher 35 anos 200 mil" → productCode DDR5G, age=35, gender female, capital=200000
   - "premio do seguro de vida para mulher de 28 anos, capital R$ 100.000" → age=28, capital=100000
   - variações de capital: "500 mil", "R$ 500.000", "500000", "1 milhao" (se suportado — senão FINDING)
   - DIT/renda: "DIT MAG renda mensal 5000" → rendaMensal=5000
   - franquia: casos com franquia se o parser extrai
2. NEGATIVOS (hasIntent=false): perguntas CONCEITUAIS que NÃO são cotação —
   "o que cobre o seguro de vida da MAG?", "carencia da Prudential", "quais exclusoes da Azos"
   (essas devem ir pro oráculo, não pro fast-path).
3. EDGE: idade sem capital, capital sem idade, gênero implícito ("para ele"/"para ela"),
   números por extenso, idade fora de faixa (0, 120).
Mínimo 20 casos. Cada mismatch entre esperado e obtido → FINDING documentado, não falha dura.
</action>
<acceptance_criteria>
- app/scripts/phase2/rate-intent.test.ts existe, importa detectRateIntent de @/services/rag/rate-lookup
- >= 20 casos (positivos + negativos + edge)
- script "phase2:rate-intent:test" no package.json
- `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/rate-intent.test.ts` exita 0 (findings documentados não contam como failed)
- se houver findings: app/scripts/phase2/FINDINGS-ciclo003.md criado com os detalhes
</acceptance_criteria>
</task>

<task id="2">
<name>TST-02 — formatRateAnswer + math (rate-answer.test.ts)</name>
<read_first>
- app/src/services/rag/rate-lookup.ts (formatRateAnswer linha 616; helpers de math/unidade linhas 739-806; rate_units: fixed_brl_monthly, per_1000_monthly, per_1000_annual, per_100_diaria_monthly, per_1000_renda_monthly)
- app/scripts/phase2/rate-unit-guard.test.ts (já cobre 2 casos de math — NÃO duplicar, estender)
</read_first>
<action>
Criar `app/scripts/phase2/rate-answer.test.ts` cobrindo formatRateAnswer com RateRow sintéticos:
- Cada um dos 5 rate_units → formato de saída correto (unidade certa, sem inversão mensal/anual)
- per_1000_monthly + capital: mensal = taxa*capital/1000, anual = mensal*12 (asserir os dois)
- per_1000_annual + capital: anual = taxa*capital/1000, mensal = anual/12
- comparativo (>=2 rows mesma unidade): identifica o mais barato corretamente + spread
- linha única (1 row): formato sem comparativo
- fixed_brl_monthly: sem cálculo de capital (valor fixo)
- per_100_diaria_monthly e per_1000_renda_monthly: rótulo de unidade correto
Asserir contra strings/valores EXATOS calculados à mão (sem reimplementar a fórmula no teste —
asserir o resultado numérico final esperado).
</action>
<acceptance_criteria>
- rate-answer.test.ts existe, cobre os 5 rate_units + capital + comparativo + linha única
- script "phase2:rate-answer:test" no package.json
- teste exita 0 (ou findings documentados)
</acceptance_criteria>
</task>

<task id="3">
<name>TST-03+04 — citation.ts + context-builder.ts</name>
<read_first>
- app/src/services/rag/citation.ts (parse/validação [N], índice inválido)
- app/src/services/rag/context-builder.ts (formatBlock linha 153; buildContext, stitching, truncação por orçamento de chars)
</read_first>
<action>
Criar `app/scripts/phase2/citation.test.ts`:
- citações [1][2] válidas mapeiam pras fontes certas
- citação [9] com só 3 fontes → detectada como inválida (invalidCitationIndexes)
- texto sem citação → coverage 0
- citação duplicada / fora de ordem
Criar `app/scripts/phase2/context-builder.test.ts`:
- formatBlock monta cabeçalho [N] Seguradora — Produto | Documento | Página + Processo SUSEP + Fonte + content
- buildContext respeita orçamento de chars (trunca quando excede; não corta no meio de bloco de forma quebrada)
- chunk stitching (se context-builder une chunks adjacentes — testar o comportamento real)
Usar SearchResult/ContextBlock sintéticos (ler os tipos).
</action>
<acceptance_criteria>
- citation.test.ts e context-builder.test.ts existem, com scripts no package.json
- ambos exitam 0 (ou findings documentados)
</acceptance_criteria>
</task>

<task id="4">
<name>TST-05 — query-decomposer + query-expansion (query-transforms.test.ts)</name>
<read_first>
- app/src/services/rag/query-decomposer.ts (detecção comparativa, sub_queries, is_comparative)
- app/src/services/rag/query-expansion.ts (jargon expansion)
</read_first>
<action>
Criar `app/scripts/phase2/query-transforms.test.ts`:
- decomposer: "compare Prudential e MAG vida inteira" → is_comparative=true, sub_queries por seguradora
- decomposer: pergunta simples → is_comparative=false
- expansion: jargão "majorada" / "IPA" expandido corretamente; pergunta sem jargão passa intacta
Só funções PURAS — se alguma depender de LLM call, testar só a parte determinística ou marcar como
fora de escopo no FINDINGS (não mockar LLM neste ciclo).
</action>
<acceptance_criteria>
- query-transforms.test.ts existe, script no package.json
- exita 0 (ou findings documentados)
- partes que dependem de LLM ficam anotadas como fora de escopo, não mockadas
</acceptance_criteria>
</task>

</tasks>

<must_haves>
truths:
  - "detectRateIntent tem corpus de teste; mismatches viram findings documentados, não silêncio"
  - "formatRateAnswer cobre os 5 rate_units com math correta (sem inversão mensal/anual)"
  - "Nenhum código de produção alterado, exceto fix justificado por bug revelado (com finding)"
artifacts:
  - path: app/scripts/phase2/rate-intent.test.ts
    contains: "detectRateIntent"
  - path: app/scripts/phase2/rate-answer.test.ts
    contains: "formatRateAnswer"
key_links:
  - from: app/scripts/phase2/rate-intent.test.ts
    via: "@/services/rag/rate-lookup"
    to: app/src/services/rag/rate-lookup.ts
</must_haves>

<verification>
1. Cada um dos 5 testes roda via `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/<f>.test.ts` e exita 0.
2. `npm run build` exit 0 (testes não devem quebrar tipos).
3. FINDINGS-ciclo003.md lista qualquer divergência código-vs-esperado encontrada (especialmente em detectRateIntent).
4. Total de casos >= ~50 somando os 5 arquivos.
</verification>
