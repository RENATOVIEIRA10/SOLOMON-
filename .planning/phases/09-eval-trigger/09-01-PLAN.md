---
phase: 9
plan: 09-01
wave: 1
depends_on: []
autonomous: true
requirements: [EVAL-TRIGGER-01]
files_modified:
  - app/src/lib/auth.ts
  - app/src/app/api/admin/evals/trigger/route.ts
  - app/src/app/api/admin/evals/jobs/route.ts
  - app/src/app/(app)/admin/page.tsx
  - app/src/components/admin/eval-trigger.tsx
  - app/src/components/admin/eval-dashboard.tsx
  - app/eval/ragas/poll_eval_jobs.py
  - docs/ops/eval-trigger-queue.md
---

<objective>
Fechar o item 4 do ciclo 002: disparar e monitorar evals Ragas pela web sem SSH manual.
Padrão fila (sem RCE): admin enfileira job na tabela `eval_jobs` do agentes-hub →
poller cron na VPS reivindica e roda `run_eval.py` → dashboard mostra status.

Tabela `eval_jobs` JÁ CRIADA no hub (zwnlpumonvkrghoxnddd):
  id uuid pk | project text='solomon' | status text CHECK(requested|running|done|failed)
  | params jsonb | requested_by text | run_id text | error text | created_at | updated_at
  RLS on, sem políticas (só service_role acessa, igual eval_runs). Trigger updated_at automático.
</objective>

<security_context>
Disparar eval custa dinheiro (judge API) e roda processo na VPS. O gate é ADMIN,
não apenas "broker allowlisted". Endpoint NUNCA executa shell vindo da web — só
escreve uma linha. O poller na VPS é quem executa, e só roda comando fixo
(run_eval.py) com params validados/sanitizados.
</security_context>

<tasks>

<task id="1">
<name>requireAdmin() — gate de admin</name>
<read_first>
- app/src/lib/auth.ts (requireAuthUserId, isAllowlisted, allowlist, forbidden — replicar padrão)
</read_first>
<action>
Em app/src/lib/auth.ts adicionar:
1. `function adminEmails(): Set<string>` — lê `process.env.SOLOMON_ADMIN_EMAILS` (csv,
   lowercase), mesmo padrão de `allowlist()`.
2. `export function isAdmin(email: string | null): boolean` — true se email (lowercase)
   está em adminEmails(); se SOLOMON_ADMIN_EMAILS vazio, retorna FALSE (admin é
   opt-in explícito — diferente do allowlist que libera todos quando vazio).
3. `export async function requireAdmin(): Promise<{id:string; email:string} | NextResponse>`
   — obtém o user da sessão (mesmo helper de requireAuthUserId), se não admin retorna
   `forbidden('admin only')` (403). Reusar getAuthUser existente.
</action>
<acceptance_criteria>
- auth.ts contém `export function isAdmin` e `export async function requireAdmin`
- isAdmin retorna false quando SOLOMON_ADMIN_EMAILS não definido (grep o comportamento)
- npm run build exit 0
</acceptance_criteria>
</task>

<task id="2">
<name>Endpoints: POST trigger + GET jobs (admin-gated)</name>
<read_first>
- app/src/app/api/admin/evals/route.ts (padrão: createHubClient, project='solomon', try/catch)
- app/src/lib/supabase-hub.ts (createHubClient — service role)
- app/src/lib/auth.ts (requireAdmin da task 1)
- app/eval/ragas/run_eval.py (args válidos: --limit, --judge via JUDGE_BACKEND, --multi-judge, --skip-ragas)
</read_first>
<action>
1. Criar `app/src/app/api/admin/evals/trigger/route.ts` (POST, revalidate 0):
   - `const auth = await requireAdmin(); if (auth instanceof NextResponse) return auth;`
   - Ler body: `{ limit?: number; judge?: 'openai'|'gemini'|'anthropic'; multiJudge?: boolean }`.
   - VALIDAR: limit inteiro 1..50 (default 49); judge ∈ whitelist {openai,gemini,anthropic}
     (default 'openai'); multiJudge boolean. Rejeitar 400 fora disso — NUNCA passar string
     livre adiante (o poller usa esses valores em comando).
   - ANTI-DUPLA-FILA: contar eval_jobs com status IN ('requested','running') project='solomon';
     se >0, retornar 409 "já existe job ativo".
   - Inserir linha: status='requested', params={limit,judge,multiJudge}, requested_by=auth.email.
     Retornar {id, status} 201.
2. Criar `app/src/app/api/admin/evals/jobs/route.ts` (GET, revalidate 0):
   - requireAdmin gate.
   - Retornar últimas 10 linhas de eval_jobs (project solomon) ordenadas created_at desc:
     id, status, params, requested_by, run_id, error, created_at, updated_at.
</action>
<acceptance_criteria>
- trigger/route.ts contém requireAdmin, validação de limit (1..50) e whitelist de judge, e o check 409 de job ativo
- jobs/route.ts contém requireAdmin e select de eval_jobs
- Nenhum dos dois interpola input do usuário em string de shell/SQL cru (usa client param)
- npm run build exit 0
</acceptance_criteria>
</task>

<task id="3">
<name>UI: botão disparar + painel de status (polling)</name>
<read_first>
- app/src/components/admin/eval-dashboard.tsx (componente existente — onde encaixar)
- app/src/app/(app)/admin/page.tsx (server component — adicionar gate isAdmin)
- app/src/components/dashboard/* (padrão visual Card, mono-tag, gold; sem azul)
</read_first>
<action>
1. admin/page.tsx: no topo, após obter o user, checar isAdmin — se não admin, renderizar
   um aviso "Área restrita" (não vazar dados de eval pra não-admin). Importar getAuthUser/isAdmin.
   Passar `isAdmin` boolean pro EvalDashboard.
2. Criar `app/src/components/admin/eval-trigger.tsx` ("use client"):
   - Card com select de limit (3 smoke / 49 full), select de judge (openai/gemini/anthropic),
     toggle multi-judge, botão "Disparar avaliação".
   - POST /api/admin/evals/trigger; ao enfileirar, inicia polling de GET /api/admin/evals/jobs
     a cada 8s; mostra o job ativo com status (requested→running→done/failed), run_id quando done.
   - Para o polling quando status done/failed. Estética SOLOMON (gold, mono-tag, sem azul,
     sem emoji como ícone estrutural — usar lucide).
3. eval-dashboard.tsx: renderizar <EvalTrigger/> no topo quando isAdmin.
</action>
<acceptance_criteria>
- eval-trigger.tsx existe, faz POST trigger + polling de jobs, sem cor azul
- admin/page.tsx checa isAdmin e não renderiza dados de eval para não-admin
- npm run build exit 0
</acceptance_criteria>
</task>

<task id="4">
<name>Poller VPS — claim atômico + run + status</name>
<read_first>
- app/eval/ragas/run_eval.py (CLI: --limit, --multi-judge; JUDGE_BACKEND via env; gera run_id)
- app/scripts/phase2/build-sft-v2-dataset.py (padrão de script VPS: urllib, env, sem deps extras)
</read_first>
<action>
Criar `app/eval/ragas/poll_eval_jobs.py` (Python stdlib + requests-via-urllib ao REST do hub,
OU psql se disponível — preferir REST com SUPABASE service key já presente em /root/agents/config/.env):
1. Buscar 1 job 'requested' mais antigo (project solomon).
2. CLAIM ATÔMICO: PATCH status='running' WHERE id=X AND status='requested' (condição na query
   evita corrida com outra instância do poller). Se 0 linhas afetadas, sai (outro pegou).
3. Montar comando: `python run_eval.py --limit <params.limit>` + `--multi-judge` se params.multiJudge;
   env JUDGE_BACKEND=<params.judge>. Os params vêm validados do app, mas o poller AINDA assim
   só aceita limit inteiro e judge na whitelist {openai,gemini,anthropic} — defesa em profundidade
   (nunca confiar; rejeitar job malformado marcando failed).
4. Rodar; capturar o run_id (o run_eval grava no eval_runs com timestamp; ler o timestamp gerado
   do stdout/results-dir). On success: PATCH status='done', run_id=<ts>. On erro/exit!=0:
   PATCH status='failed', error=<últimas linhas>.
5. Idempotente e seguro pra rodar a cada 5 min via cron. Documentar a linha de cron.
NÃO adicionar o cron automaticamente (operação na VPS) — entregar o script + a linha de cron
documentada em docs/ops/eval-trigger-queue.md para o CEO instalar com 1 comando.
</action>
<acceptance_criteria>
- poll_eval_jobs.py existe; faz claim condicional (status='requested' na cláusula de update)
- poller revalida limit inteiro e judge na whitelist antes de executar (defesa em profundidade)
- docs/ops/eval-trigger-queue.md documenta schema, fluxo, a linha de crontab e como instalar
- python -c "import ast; ast.parse(open('app/eval/ragas/poll_eval_jobs.py').read())" exit 0 (sintaxe)
</acceptance_criteria>
</task>

</tasks>

<threat_model>
- Endpoint disparando custo/processo: gate requireAdmin (opt-in via SOLOMON_ADMIN_EMAILS) + anti-dupla-fila 409 + limit cap 50.
- Injeção via params no poller: app valida (whitelist judge, limit int) E poller revalida (defesa em profundidade); nada de string livre em comando.
- Corrida entre pollers: claim atômico condicional (UPDATE ... WHERE status='requested').
- Dados de eval vazando pra não-admin: admin/page.tsx gate isAdmin antes de renderizar.
- Sem inbound RCE: web só escreve linha; execução é exclusiva do poller VPS com comando fixo.
</threat_model>

<must_haves>
truths:
  - "Disparar eval pela web só funciona para admin (SOLOMON_ADMIN_EMAILS); broker comum recebe 403/área restrita"
  - "Web nunca executa shell; só enfileira. Poller VPS executa comando fixo com params validados 2x"
  - "Não há dois evals na fila ao mesmo tempo (409)"
  - "Dashboard mostra requested→running→done/failed e linka o run_id"
artifacts:
  - path: app/src/app/api/admin/evals/trigger/route.ts
    contains: "requireAdmin"
  - path: app/eval/ragas/poll_eval_jobs.py
    contains: "requested"
key_links:
  - from: app/src/app/api/admin/evals/trigger/route.ts
    via: "requireAdmin"
    to: app/src/lib/auth.ts
  - from: app/src/components/admin/eval-trigger.tsx
    via: "/api/admin/evals/trigger"
    to: app/src/app/api/admin/evals/trigger/route.ts
</must_haves>

<verification>
1. npm run build exit 0; grep-checks dos acceptance.
2. Sem SOLOMON_ADMIN_EMAILS setado, POST trigger → 403 (admin opt-in).
3. Pós-deploy + setar SOLOMON_ADMIN_EMAILS no Vercel: admin dispara smoke (limit 3),
   poller na VPS roda, dashboard vira done com run_id, eval_runs ganha o run.
4. Segundo disparo enquanto um roda → 409.
</verification>
