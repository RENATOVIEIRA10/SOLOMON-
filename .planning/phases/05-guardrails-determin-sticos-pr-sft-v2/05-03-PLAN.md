---
phase: 05-guardrails-determin-sticos-pr-sft-v2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/services/rag/pre-sinistro.ts
  - app/scripts/phase2/pre-sinistro-h11-guard.test.ts
autonomous: true
requirements: [GRD-04]
must_haves:
  truths:
    - "Pre-sinistro forca veredicto RISCO quando NEM cobertura NEM exclusao tem cláusula aplicável recuperada (caso H11)"
    - "Presuncao de cobertura sem cláusula aplicável e impossivel por construcao"
    - "Texto sintetico de downgrade vai SEMPRE em riskFlags, NUNCA em rationale (licao PR #64)"
  artifacts:
    - path: "app/src/services/rag/pre-sinistro.ts"
      provides: "post-validation block (linhas ~284-307) confirmado/reforcado + hasEvidenceFor exportado para teste"
      contains: "hasEvidenceFor"
    - path: "app/scripts/phase2/pre-sinistro-h11-guard.test.ts"
      provides: "teste H11: results sem keyword de cobertura nem exclusao → ambos hasEvidenceFor falsos → downgrade RISCO"
      contains: "hasEvidenceFor"
  key_links:
    - from: "app/src/services/rag/pre-sinistro.ts post-validation"
      to: "hasEvidenceFor('COBERTO') / hasEvidenceFor('NAO_COBERTO')"
      via: "downgrade para RISCO quando evidencia ausente"
      pattern: "verdict = \"RISCO\""
---

<objective>
GRD-04 (caso H11 — manter analise sem suporte inconclusiva): O bloco de post-validation determinística em `pre-sinistro.ts` (linhas ~284-307, da PR #64) JA existe. Esta fase NAO reconstroi — verifica que o comportamento atual cobre H11, fecha qualquer gap, e adiciona cobertura de regressao com teste unitario.

Logica canonica a preservar (verificada no codigo):
- `hasEvidenceFor("COBERTO", results)` busca COVERAGE_KEYWORDS nos primeiros 1500 chars de cada chunk
- `hasEvidenceFor("NAO_COBERTO", results)` busca EXCLUSION_KEYWORDS
- Se `verdict === "COBERTO"` e `!hasEvidenceFor("COBERTO")` → downgrade para RISCO + finalConfidence ≤ 0.45 + riskFlag
- Se `verdict === "NAO_COBERTO"` e `!hasEvidenceFor("NAO_COBERTO")` → downgrade para RISCO + riskFlag
- Resultado para H11 (nem cobertura nem exclusao): qualquer veredicto conclusivo cai para RISCO por construcao

Regra critica PR #64 (F caiu 0.63→0.39 quando violada): o texto de downgrade vai em `riskFlags`, NUNCA em `rationale`. O rationale e output puro do LLM (grounded nos chunks); o harness Ragas mede faithfulness contra o rationale e EXCLUI riskFlags.

Purpose: Eliminar a classe de falha "presuncao de cobertura sem cláusula".
Output: pre-sinistro.ts validado/reforcado (hasEvidenceFor exportavel), teste de regressao H11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-CONTEXT.md
@.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-PATTERNS.md
@docs/qa/sft-v2-model-gate-2026-06-07.md
@CLAUDE.md

<interfaces>
<!-- Verificado em pre-sinistro.ts: -->

hasEvidenceFor (pre-sinistro.ts linhas 495-504) — atualmente NAO exportada:
```typescript
function hasEvidenceFor(verdict: "COBERTO" | "NAO_COBERTO", results: SearchResult[]): boolean {
  const keywords = verdict === "COBERTO" ? COVERAGE_KEYWORDS : EXCLUSION_KEYWORDS;
  return results.some((r) => {
    const head = normalizeText(r.content).slice(0, 1500);
    return keywords.some((kw) => head.includes(kw));
  });
}
```

Post-validation block (pre-sinistro.ts linhas 279-307) — NAO quebrar, apenas confirmar:
```typescript
if (verdict !== "RISCO" && validatedCitation === null) {
  verdict = "RISCO"; finalConfidence = Math.min(finalConfidence, 0.45);
  riskFlags = addRiskFlag(riskFlags, "Veredicto conclusivo rebaixado: sem citacao literal validada nos chunks");
}
if (verdict === "COBERTO" && !hasEvidenceFor("COBERTO", results)) {
  verdict = "RISCO"; finalConfidence = Math.min(finalConfidence, 0.45);
  riskFlags = addRiskFlag(riskFlags, "Downgrade automatico: veredicto COBERTO sem chunk de cobertura explicita nos documentos indexados");
}
if (verdict === "NAO_COBERTO" && !hasEvidenceFor("NAO_COBERTO", results)) {
  verdict = "RISCO"; finalConfidence = Math.min(finalConfidence, 0.45);
  riskFlags = addRiskFlag(riskFlags, "Downgrade automatico: veredicto NAO_COBERTO sem chunk de exclusao explicita nos documentos indexados");
}
```

SearchResult (search.ts): { id; content; similarity; metadata; source_url; source_type; product_id; insurer_id }
COVERAGE_KEYWORDS / EXCLUSION_KEYWORDS / normalizeText — definidos em pre-sinistro.ts (ler para os valores exatos).

Convencao de teste: standalone tsx, ok() helper, env stubbed, process.exit(1). Ver app/scripts/phase2/rag-comparison-helpers.test.ts.
tsx confirmado presente (v4.21). Para evitar conflito de package.json com plans paralelos da Wave 1,
ESTE plano NAO edita package.json — o teste roda via `npx tsx --tsconfig scripts/tsconfig.json <file>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirmar/reforcar post-validation + exportar hasEvidenceFor + teste H11</name>
  <files>app/src/services/rag/pre-sinistro.ts, app/scripts/phase2/pre-sinistro-h11-guard.test.ts</files>
  <read_first>
    - app/src/services/rag/pre-sinistro.ts (linhas 259-320 post-validation completo; linhas 495-504 hasEvidenceFor; localizar COVERAGE_KEYWORDS, EXCLUSION_KEYWORDS, normalizeText e seus valores)
    - app/scripts/phase2/rag-comparison-helpers.test.ts (convencao de teste tsx)
    - docs/qa/sft-v2-model-gate-2026-06-07.md (caso H11: unsafe presumption of coverage without an applicable coverage clause)
    - .planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-CONTEXT.md (decisao GRD-04: licao PR #64 — texto sintetico em riskFlags, nao rationale)
  </read_first>
  <behavior>
    - hasEvidenceFor('COBERTO', chunksComCobertura) === true   (chunk contem ao menos 1 COVERAGE_KEYWORD nos primeiros 1500 chars)
    - hasEvidenceFor('NAO_COBERTO', chunksComExclusao) === true (chunk contem ao menos 1 EXCLUSION_KEYWORD)
    - hasEvidenceFor('COBERTO', chunksGenericos) === false      (chunk H11: texto sem cobertura nem exclusao)
    - hasEvidenceFor('NAO_COBERTO', chunksGenericos) === false
    - Cenario H11 combinado: para um chunk generico (sem cobertura nem exclusao), AMBOS hasEvidenceFor retornam false → qualquer veredicto COBERTO ou NAO_COBERTO seria rebaixado a RISCO pelo post-validation block
  </behavior>
  <action>
    Em app/src/services/rag/pre-sinistro.ts:

    1. CONFIRMAR (ler) que o post-validation block linhas 284-307 esta intacto e cobre os 3 casos: citation null, COBERTO sem evidencia, NAO_COBERTO sem evidencia. NAO alterar a logica de downgrade.

    2. CONFIRMAR (ler) que NENHUM dos riskFlags do downgrade e escrito em `rationale`. O rationale (linha 261-264) e derivado apenas de `parsed.rationale` (output do LLM). Se houver QUALQUER concatenacao de texto sintetico de downgrade em rationale, REMOVER e mover para addRiskFlag (regra PR #64).

    3. Exportar hasEvidenceFor: trocar `function hasEvidenceFor(` por `export function hasEvidenceFor(` na linha 495. Isso permite teste unitario direto sem chamar o LLM.

    4. Criar app/scripts/phase2/pre-sinistro-h11-guard.test.ts na convencao tsx:
       - env stubs no topo
       - import { hasEvidenceFor } from '@/services/rag/pre-sinistro'
       - import type { SearchResult } from '@/services/rag/search'
       - helper ok()
       - construir 3 fixtures de SearchResult (objetos completos: id, content, similarity, metadata:{}, source_url:null, source_type:'pdf', product_id:null, insurer_id:'test'):
         * chunkCobertura.content = trecho contendo uma COVERAGE_KEYWORD real (ler o array no codigo e usar uma das palavras, ex: 'cobertura', 'garantia', 'indeniza')
         * chunkExclusao.content = trecho contendo uma EXCLUSION_KEYWORD real (ex: 'exclui', 'nao cobre', 'carencia')
         * chunkGenerico.content = trecho neutro sobre H11 SEM nenhuma keyword (ex: 'O presente contrato foi celebrado entre as partes na data indicada na proposta.')
       - gate functions cobrindo os 5 casos de <behavior>
       - `if (failed > 0) { console.error(...); process.exit(1) }; console.log(\`${passed} passed\`)`

    NAO editar package.json nesta tarefa. O teste roda via tsx direto (ver <verify>).
  </action>
  <verify>
    <automated>cd app && npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/pre-sinistro-h11-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O comando `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/pre-sinistro-h11-guard.test.ts` (rodado de app/) sai com codigo 0 e imprime "passed"
    - grep `export function hasEvidenceFor` em app/src/services/rag/pre-sinistro.ts retorna 1 match
    - grep `verdict = "RISCO"` em app/src/services/rag/pre-sinistro.ts retorna >=3 matches (os 3 downgrades preservados)
    - O teste confirma que chunkGenerico (H11) retorna false em AMBOS hasEvidenceFor('COBERTO') e hasEvidenceFor('NAO_COBERTO')
    - O rationale em pre-sinistro.ts NAO contem concatenacao de texto de downgrade (nenhuma string de riskFlag aparece sendo atribuida a rationale)
  </acceptance_criteria>
  <done>hasEvidenceFor exportada e testada; post-validation block confirmado intacto com 3 downgrades; teste H11 prova que chunk generico forca RISCO; rationale sem texto sintetico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM (Sonnet 4.6) → veredicto pre-sinistro | output do LLM pode presumir cobertura sem cláusula aplicável |
| veredicto → corretor (decisao juridica) | veredicto COBERTO sem suporte e dano de alta consequencia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | post-validation verdict em pre-sinistro.ts | mitigate | hasEvidenceFor verifica keyword nos chunks reais; veredicto conclusivo sem evidencia cai para RISCO em codigo — LLM nao pode forcar COBERTO sem cláusula |
| T-05-08 | Information disclosure | riskFlags com texto sintetico | accept | riskFlags sao excluidos do harness Ragas e exibidos ao corretor como alerta legitimo; nao vazam dados sensiveis. Rationale permanece grounded (regra PR #64) |
| T-05-09 | Repudiation | downgrade sem registro | mitigate | Cada downgrade adiciona riskFlag descritivo (addRiskFlag dedupe-safe) — trilha auditavel do motivo do RISCO |
</threat_model>

<verification>
- `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/pre-sinistro-h11-guard.test.ts` passa (5 assertions, H11 forca RISCO)
- grep confirma 3 downgrades para RISCO preservados e hasEvidenceFor exportada
- rationale nao contem texto sintetico de downgrade
</verification>

<success_criteria>
Pre-sinistro forca RISCO/inconclusivo quando nem cobertura nem exclusao tem cláusula aplicável recuperada (H11). Presuncao de cobertura sem cláusula e impossivel por construcao. Regra PR #64 respeitada (texto de downgrade em riskFlags, nao rationale).
</success_criteria>

<output>
After completion, create `.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-03-SUMMARY.md`
</output>
