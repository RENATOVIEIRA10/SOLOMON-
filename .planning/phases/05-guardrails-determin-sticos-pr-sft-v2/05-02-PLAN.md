---
phase: 05-guardrails-determin-sticos-pr-sft-v2
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/src/services/rag/domain-guard.ts
  - app/src/services/rag/answer.ts
  - app/src/services/rag/stream.ts
  - app/scripts/phase2/domain-guard.test.ts
autonomous: true
requirements: [GRD-02, GRD-03]
must_haves:
  truths:
    - "Pergunta sobre seguradora cujos chunks recuperados nao correspondem (insurer_id mismatch) retorna recusa explicita ANTES de chamar o LLM (caso H05)"
    - "Pergunta fora do dominio vida/pessoas (auto, residencial, viagem) recebe recusa padronizada ANTES de qualquer retrieval (caso H09)"
    - "As mesmas recusas valem no path SSE (stream.ts)"
  artifacts:
    - path: "app/src/services/rag/domain-guard.ts"
      provides: "detectOutOfDomainQuery() determinístico (keyword/regex) + mensagens de recusa padronizadas"
      contains: "detectOutOfDomainQuery"
    - path: "app/src/services/rag/answer.ts"
      provides: "early-return de dominio (apos detectInsurers) + early-return de insurer-mismatch (apos loadEnrichment)"
      contains: "detectOutOfDomainQuery"
    - path: "app/src/services/rag/stream.ts"
      provides: "mesmos guardrails como yield token+meta+return"
      contains: "detectOutOfDomainQuery"
    - path: "app/scripts/phase2/domain-guard.test.ts"
      provides: "teste de detectOutOfDomainQuery cobrindo H09 + falso-positivo (vida nao e bloqueada)"
      contains: "detectOutOfDomainQuery"
  key_links:
    - from: "app/src/services/rag/answer.ts"
      to: "detectOutOfDomainQuery"
      via: "chamada logo apos detectInsurers, early-return se isOutOfDomain"
      pattern: "detectOutOfDomainQuery\\("
    - from: "app/src/services/rag/answer.ts"
      to: "searchResults insurer_id vs requestedIds"
      via: "check de mismatch apos loadEnrichment, early-return se nenhum chunk casa"
      pattern: "retrievedIds"
---

<objective>
GRD-02 + GRD-03: Dois early-returns determinísticos ANTES da geracao, ambos no orquestrador (answer.ts + stream.ts).

GRD-02 (caso H05 — recusar fonte ausente): Quando a pergunta menciona uma seguradora X mas NENHUM chunk recuperado tem `insurer_id` ∈ ids(X), retornar recusa explicita ("nao tenho a fonte X indexada") ANTES de chamar o LLM. Sem fallback silencioso para fonte de outra seguradora (o candidato Nova Pro inventou condicoes da Porto). Hoje isso e apenas uma instrucao no SYSTEM_PROMPT_TEMPLATE (passo 4) — GRD-02 move para codigo determinístico.

GRD-03 (caso H09 — fronteira de dominio): Pergunta de auto/residencial/viagem recebe recusa padronizada ANTES de qualquer retrieval, via classificador determinístico de keyword/regex (`detectOutOfDomainQuery`), seguindo o shape de `detectRateIntent`. Hoje `excludeNonLifeProductTypes` filtra chunks non-life do corpus, mas NAO bloqueia a pergunta — o LLM pode receber contexto de vida e responder sobre auto com conteudo inventado.

Purpose: Eliminar as classes de falha "fonte de seguradora errada" e "fuga de dominio".
Output: domain-guard.ts (utility novo), early-returns em answer.ts + stream.ts, teste de regressao.

NOTA dependencia: este plano edita answer.ts e stream.ts, os mesmos arquivos do Plan 01. Por isso roda na Wave 2, apos 05-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-CONTEXT.md
@.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-PATTERNS.md
@docs/qa/sft-v2-model-gate-2026-06-07.md
@CLAUDE.md

<interfaces>
<!-- Verificado no codigo: -->

resolveInsurerIds (answer.ts linha 1081): export async function resolveInsurerIds(canonicalNames: string[]): Promise<Map<string, string[]>>
  // retorna Map<nome canonico, string[] de UUIDs>

SearchResult (search.ts linhas 21-30): { id; content; similarity; metadata; source_url; source_type; product_id; insurer_id: string | null }

AskResult shape para early-return (answer.ts linhas 130-158, usado no fast-path 273-291):
```typescript
return {
  answer,                       // string com recusa
  citations: [],
  sources: [],
  model: 'domain-guard',        // ou 'insurer-source-guard'
  tokensUsed: 0,
  latencyMs: Date.now() - startTime,
  conversationId,               // opcional
  confidenceScore: 1.0,         // recusa determinística e "certa"
  avgSimilarity: 0,
  sourceCount: 0,
  lowConfidence: false,
  citationCoverage: 1,
  invalidCitationIndexes: [],
  answerWarnings: [],
}
```

stream.ts early-return = yield {type:'token', delta: answer} + yield {type:'meta', model, conversationId, citations:[], tokensUsed:0, latencyMs, confidenceScore, avgSimilarity, sourceCount:0, lowConfidence:false, citationCoverage:1, invalidCitationIndexes:[], answerWarnings:[]} + return.

answer.ts pontos de insercao:
  - detectInsurers em linha 172 → inserir GRD-03 (domain) logo apos
  - loadEnrichment em linha 516 → inserir GRD-02 (insurer-mismatch) logo apos, antes de buildContext (linha 528)
  - resolveInsurerIds(mentionedInsurers) JA e chamado em linha 317 (reutilizar resultado se acessivel, nao duplicar query)

stream.ts pontos de insercao:
  - detectInsurers em linha 75 → GRD-03 apos
  - loadEnrichment em linha 216 → GRD-02 apos, antes de buildContext (linha 225)
  - resolveInsurerIds JA chamado em linha 180

Vocabulario out-of-domain (search.ts ~linha 322, lista excludeNonLifeProductTypes):
  ['PGBL', 'VGBL', 'previdencia', 'capitalizacao', 'residencial', 'viagem', 'auto']
  (PGBL/VGBL/previdencia/capitalizacao sao previdencia — NAO bloquear como out-of-domain;
   o dominio suportado inclui vida/pessoas. Bloquear apenas: auto, residencial, viagem.)

detectRateIntent shape (rate-lookup.ts) — padrao a copiar:
```typescript
export function detectRateIntent(question: string, insurer?: string): RateIntent {
  const q = stripAccents(question.toLowerCase())
  const hasRateKeyword = RATE_KEYWORDS.some((kw) => q.includes(kw))
  if (!hasRateKeyword) return { hasIntent: false }
  ...
}
```

Tom de recusa ja estabelecido (SYSTEM_PROMPT_TEMPLATE passo 4):
  "Nao encontrei condicoes gerais da [X] na base para responder isso com seguranca."

Convencao de teste: standalone tsx, ok() helper, env stubbed, process.exit(1). Ver app/scripts/phase2/rag-comparison-helpers.test.ts.
tsx confirmado presente (v4.21). Para evitar conflito de package.json, ESTE plano NAO edita package.json —
o teste roda via `npx tsx --tsconfig scripts/tsconfig.json <file>`. Registrar script npm e follow-up opcional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar domain-guard.ts com detectOutOfDomainQuery + teste</name>
  <files>app/src/services/rag/domain-guard.ts, app/scripts/phase2/domain-guard.test.ts</files>
  <read_first>
    - app/src/services/rag/search.ts (linha ~322, lista excludeNonLifeProductTypes — vocabulario de dominios)
    - app/src/services/rag/rate-lookup.ts (linhas 69-151: RATE_KEYWORDS + detectRateIntent + stripAccents — padrao a copiar)
    - app/scripts/phase2/rag-comparison-helpers.test.ts (convencao de teste tsx)
  </read_first>
  <behavior>
    - detectOutOfDomainQuery('Quanto custa seguro de auto da Porto?') → { isOutOfDomain: true, detectedDomain: 'auto' }
    - detectOutOfDomainQuery('seguro do meu carro') → { isOutOfDomain: true, detectedDomain: 'auto' }
    - detectOutOfDomainQuery('seguro residencial cobre incendio?') → { isOutOfDomain: true, detectedDomain: 'residencial' }
    - detectOutOfDomainQuery('seguro viagem internacional') → { isOutOfDomain: true, detectedDomain: 'viagem' }
    - detectOutOfDomainQuery('qual a cobertura de morte do Vida Viva?') → { isOutOfDomain: false }  (vida NAO bloqueada)
    - detectOutOfDomainQuery('invalidez por acidente cobre o que?') → { isOutOfDomain: false }
    - detectOutOfDomainQuery('seguro de vida tem carencia?') → { isOutOfDomain: false }
    - refusalMessageForDomain('auto') retorna string contendo 'vida' e 'auto' (recusa honesta, sem vazar estrutura interna)
  </behavior>
  <action>
    Criar app/src/services/rag/domain-guard.ts:

    ```typescript
    /** GRD-03: fronteira de dominio (vida/pessoas) imposta ANTES da geracao. */

    function stripAccentsLower(s: string): string {
      return s.normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase()
    }

    const OUT_OF_DOMAIN_PATTERNS: Array<{ domain: string; pattern: RegExp }> = [
      { domain: 'auto', pattern: /\b(seguro\s+(de\s+)?auto|seguro\s+automovel|automovel|meu\s+carro|do\s+carro|veiculo|guincho|colisao)\b/ },
      { domain: 'residencial', pattern: /\b(seguro\s+residencial|seguro\s+da\s+casa|seguro\s+do\s+(apto|apartamento|imovel)|incendio\s+residencial)\b/ },
      { domain: 'viagem', pattern: /\b(seguro\s+(de\s+)?viagem|assistencia\s+(de\s+)?viagem|seguro\s+viagem)\b/ },
    ]

    export interface DomainCheck {
      isOutOfDomain: boolean
      detectedDomain?: string
    }

    export function detectOutOfDomainQuery(question: string): DomainCheck {
      const q = stripAccentsLower(question)
      for (const { domain, pattern } of OUT_OF_DOMAIN_PATTERNS) {
        if (pattern.test(q)) return { isOutOfDomain: true, detectedDomain: domain }
      }
      return { isOutOfDomain: false }
    }

    const DOMAIN_LABELS: Record<string, string> = {
      auto: 'seguro de automovel',
      residencial: 'seguro residencial',
      viagem: 'seguro viagem',
    }

    export function refusalMessageForDomain(detectedDomain?: string): string {
      const label = detectedDomain ? (DOMAIN_LABELS[detectedDomain] ?? detectedDomain) : 'esse ramo'
      return `Eu cubro apenas seguros de vida e de pessoas (vida, invalidez, doencas graves, DIT/DITA, pensao, funeral). ` +
             `Sua pergunta parece ser sobre ${label}, que esta fora do meu dominio. Nao tenho fontes indexadas para responder isso com seguranca.`
    }
    ```

    IMPORTANTE (T-05-06): a mensagem nao deve listar nomes internos de tabelas nem produtos. Manter o texto acima.

    Criar app/scripts/phase2/domain-guard.test.ts na convencao tsx (env stubs no topo, ok() helper, import { detectOutOfDomainQuery, refusalMessageForDomain } from '@/services/rag/domain-guard'), com gate functions cobrindo os 8 casos de <behavior>, e `if (failed > 0) process.exit(1)`.

    NAO editar package.json nesta tarefa. O teste roda via tsx direto (ver <verify>).
  </action>
  <verify>
    <automated>cd app && npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/domain-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - O comando `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/domain-guard.test.ts` (rodado de app/) sai com codigo 0 e imprime "passed"
    - grep `export function detectOutOfDomainQuery` em app/src/services/rag/domain-guard.ts retorna 1 match
    - grep `export function refusalMessageForDomain` em app/src/services/rag/domain-guard.ts retorna 1 match
    - O teste confirma que pergunta de vida NAO e bloqueada (isOutOfDomain === false para 'cobertura de morte do Vida Viva')
  </acceptance_criteria>
  <done>domain-guard.ts exporta detectOutOfDomainQuery + refusalMessageForDomain; teste passa com H09 e falso-positivo de vida coberto.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GRD-03 (domain) + GRD-02 (insurer-mismatch) em answer.ts e stream.ts</name>
  <files>app/src/services/rag/answer.ts, app/src/services/rag/stream.ts</files>
  <read_first>
    - app/src/services/rag/answer.ts (linhas 165-320 inicio de ask() + detectInsurers + resolveInsurerIds linha 317; linhas 515-528 loadEnrichment + buildContext; linhas 273-291 shape de early-return)
    - app/src/services/rag/stream.ts (linhas 67-225: askStream inicio, detectInsurers linha 75, resolveInsurerIds linha 180, loadEnrichment linha 216, buildContext linha 225; linhas 143-163 shape de yield token+meta)
    - app/src/services/rag/domain-guard.ts (criado na Task 1 — assinaturas)
  </read_first>
  <action>
    Em app/src/services/rag/answer.ts:

    1. Adicionar import no topo: `import { detectOutOfDomainQuery, refusalMessageForDomain } from './domain-guard'`

    2. GRD-03 — logo APOS `const mentionedInsurers = detectInsurers(question)` (linha 172), antes de qualquer retrieval/fast-path:
    ```typescript
    const domainCheck = detectOutOfDomainQuery(question)
    if (domainCheck.isOutOfDomain) {
      console.log(`[grd-03] Out-of-domain query bloqueada: domain=${domainCheck.detectedDomain}`)
      const answer = refusalMessageForDomain(domainCheck.detectedDomain)
      let conversationId: string | undefined
      if (options?.brokerId) {
        conversationId = await saveConversation({ brokerId: options.brokerId, channel: options.channel ?? 'api', message: question, response: answer, model: 'domain-guard', tokensUsed: 0, latencyMs: Date.now() - startTime, sources: [] })
      }
      return { answer, citations: [], sources: [], model: 'domain-guard', tokensUsed: 0, latencyMs: Date.now() - startTime, conversationId, confidenceScore: 1.0, avgSimilarity: 0, sourceCount: 0, lowConfidence: false, citationCoverage: 1, invalidCitationIndexes: [], answerWarnings: [] }
    }
    ```

    3. GRD-02 — logo APOS `const enrichment = await loadEnrichment(searchResults)` (linha 516), antes de buildContext (linha 528):
    ```typescript
    if (mentionedInsurers.length > 0) {
      const requestedIds = new Set([...(await resolveInsurerIds(mentionedInsurers)).values()].flat())
      const retrievedIds = new Set(searchResults.map((r) => r.insurer_id).filter((id): id is string => Boolean(id)))
      const hasMatch = [...retrievedIds].some((id) => requestedIds.has(id))
      if (requestedIds.size > 0 && !hasMatch) {
        console.log(`[grd-02] Insurer source mismatch — requested=${mentionedInsurers.join(',')} nao casa com nenhum chunk recuperado. Recusando.`)
        const answer = `Nao encontrei a fonte da seguradora ${mentionedInsurers.join(' / ')} indexada para responder isso com seguranca. Nao posso usar documentos de outra seguradora como substituto.`
        let conversationId: string | undefined
        if (options?.brokerId) {
          conversationId = await saveConversation({ brokerId: options.brokerId, channel: options.channel ?? 'api', message: question, response: answer, model: 'insurer-source-guard', tokensUsed: 0, latencyMs: Date.now() - startTime, sources: [] })
        }
        return { answer, citations: [], sources: [], model: 'insurer-source-guard', tokensUsed: 0, latencyMs: Date.now() - startTime, conversationId, confidenceScore: 1.0, avgSimilarity: 0, sourceCount: 0, lowConfidence: false, citationCoverage: 1, invalidCitationIndexes: [], answerWarnings: [] }
      }
    }
    ```
    Nota: reutilizar o resultado de resolveInsurerIds ja chamado na linha 317 se a variavel (insurerIds) estiver no escopo; senao a chamada acima e aceitavel (mesma query, custo baixo). Preferir reaproveitar.

    Em app/src/services/rag/stream.ts (espelhar, mas como yield+return):

    1. Import: `import { detectOutOfDomainQuery, refusalMessageForDomain } from './domain-guard'`

    2. GRD-03 — apos `const mentionedInsurers = detectInsurers(question)` (linha 75):
    ```typescript
    const domainCheck = detectOutOfDomainQuery(question)
    if (domainCheck.isOutOfDomain) {
      console.log(`[grd-03] Out-of-domain (stream) bloqueada: domain=${domainCheck.detectedDomain}`)
      const answer = refusalMessageForDomain(domainCheck.detectedDomain)
      let conversationId: string | undefined
      if (options?.brokerId) {
        conversationId = await saveConversation({ brokerId: options.brokerId, channel: options.channel ?? 'api', message: question, response: answer, model: 'domain-guard', tokensUsed: 0, latencyMs: Date.now() - startTime, sources: [] })
      }
      yield { type: 'token', delta: answer }
      yield { type: 'meta', model: 'domain-guard', conversationId, citations: [], tokensUsed: 0, latencyMs: Date.now() - startTime, confidenceScore: 1.0, avgSimilarity: 0, sourceCount: 0, lowConfidence: false, citationCoverage: 1, invalidCitationIndexes: [], answerWarnings: [] }
      return
    }
    ```

    3. GRD-02 — apos `const enrichment = await loadEnrichment(searchResults)` (linha 216), antes de buildContext (linha 225). Mesmo check de mismatch, finalizando com yield token+meta+return e model 'insurer-source-guard' (mesma mensagem de recusa do answer.ts).
  </action>
  <verify>
    <automated>cd app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep `from './domain-guard'` em app/src/services/rag/answer.ts retorna 1 match
    - grep `from './domain-guard'` em app/src/services/rag/stream.ts retorna 1 match
    - grep `\[grd-03\]` em app/src/services/rag/answer.ts retorna match
    - grep `\[grd-02\]` em app/src/services/rag/answer.ts retorna match
    - grep `insurer-source-guard` em app/src/services/rag/answer.ts retorna match
    - grep `insurer-source-guard` em app/src/services/rag/stream.ts retorna match
    - grep `model: 'domain-guard'` em app/src/services/rag/stream.ts retorna match
    - O early-return de GRD-03 em answer.ts esta posicionado ANTES do bloco fast-path de taxa (detectOutOfDomainQuery aparece antes de detectRateIntent no arquivo)
    - `npm run build` completa sem erro de TypeScript (exit 0)
  </acceptance_criteria>
  <done>answer.ts e stream.ts recusam out-of-domain antes do retrieval e recusam insurer-mismatch antes do LLM; build passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuario (pergunta livre) → ask()/askStream() | texto nao confiavel; pode pedir ramo fora do dominio ou seguradora sem fonte |
| chunks recuperados → contexto do LLM | chunk de seguradora errada nao pode virar fonte da resposta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | GRD-02 insurer-mismatch em answer.ts/stream.ts | mitigate | Comparacao por insurer_id (UUID) dos chunks vs resolveInsurerIds — fonte de outra seguradora nunca alimenta a resposta; check em codigo antes do LLM, nao confiavel no prompt |
| T-05-05 | Elevation of privilege | prompt-injection tentando burlar domain-guard | mitigate | detectOutOfDomainQuery roda sobre a pergunta crua antes do LLM; injecao no corpo da pergunta nao remove a deteccao por regex deterministica |
| T-05-06 | Information disclosure | mensagem de recusa (refusalMessageForDomain / insurer-source-guard) | mitigate | Mensagens nao listam nomes de tabelas internas nem produtos de outras seguradoras — apenas o ramo/seguradora pedido e a fronteira de dominio |
</threat_model>

<verification>
- `npx tsx --tsconfig scripts/tsconfig.json scripts/phase2/domain-guard.test.ts` passa (H09 + falso-positivo de vida)
- `npm run build` passa sem erros TypeScript
- grep confirma os 2 early-returns posicionados nos pontos corretos (GRD-03 antes do retrieval, GRD-02 apos loadEnrichment) em answer.ts e stream.ts
</verification>

<success_criteria>
Pergunta de seguradora sem fonte correspondente retorna recusa explicita antes do LLM (H05). Pergunta de auto/residencial/viagem recebe recusa padronizada antes do retrieval (H09). Ambos valem no path SSE.
</success_criteria>

<output>
After completion, create `.planning/phases/05-guardrails-determin-sticos-pr-sft-v2/05-02-SUMMARY.md`
</output>
